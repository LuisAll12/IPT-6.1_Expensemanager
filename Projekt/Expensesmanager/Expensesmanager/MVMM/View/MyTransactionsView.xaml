<UserControl x:Class="Expensesmanager.MVMM.View.MyTransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Expensesmanager.MVMM.View"
             mc:Ignorable="d" d:DesignWidth="800" Height="500">
    <StackPanel>
        <TextBlock x:Name="Description_Block"
                   Text="Hier sehen Sie alle Transaktionen"
                   Foreground="White"
                   FontSize="28"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,10"/>
        <!--<DataGrid ItemsSource="{Binding Records}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Betrag" Binding="{Binding Betrag}" />
                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat={}{0:dd.MM.yyyy}}" />
                <DataGridTextColumn Header="Kategorie" Binding="{Binding Kategorie}" />
            </DataGrid.Columns>
        </DataGrid>-->
    </StackPanel>
</UserControl>
